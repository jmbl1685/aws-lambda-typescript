{
  "name": "aws-lambda-typescript",
  "version": "1.0.0",
  "main": "src/main.ts",
  "scripts": {
    "dev": "nodemon --config nodemon.json",
    "deploy": "serverless deploy --verbose --stage prod",
    "remove": "serverless remove --verbose --stage prod",
    "check-dev": "serverless invoke local -f httpSample",
    "check-prod": "serverless invoke -f httpSample"
  },
  "devDependencies": {
    "nodemon": "^2.0.19",
    "serverless-offline": "^9.0.0",
    "serverless-plugin-typescript": "^2.1.2",
    "typescript": "^4.7.4"
  }
}